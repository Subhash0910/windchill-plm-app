/**
 * Navigator.jsx  —  canonical re-export of NavigatorPanel.
 *
 * The roadmap names this file `Navigator.jsx`; keeping NavigatorPanel.jsx
 * as the implementation lets existing imports keep working while new code
 * can import from the canonical name.
 */
export { default } from './NavigatorPanel';
